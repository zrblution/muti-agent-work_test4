# Phase 5 Model-Path Decision Templates

These files are unfilled human-decision templates extracted from the pending model-path decision request.

- Fill exactly one template before running `phase5-validate-model-path-decision`.
- Do not treat these templates as approval records while `approver` or `rationale` is `null`.
- Do not edit config, export environment variables, submit jobs, run a model, run a benchmark, or write `raw_outputs.jsonl` from these templates.
